"""
一、校园网拨号
服务器IP：202.193.160.123
建立连接数：4
连接端口表：
本地端口	       --	       5200	         5201        (经测试两个端口可以随意)
服务器端口	   80	       5300	         5301
作用     	连接服务器	 连接和关闭	获取使用流量和余额信息

IPClient包标记（每个包开始两个字节）：0x82,0x23
序号标记（每个包的第三个字节）num   ：0x05,0x06，0x1f，0x20,0x21,0x22,0x1E，0x14,0x15,0x16,0x17（每个都有具体作用）
数据包长度：目标端口：5300，源端口：5200 的包长度为300字节；
           目标端口：5301，源端口：5201 的包长度为500字节；
           客户端发送的包结尾用0xff填充补齐；
           服务器发送的包结尾用0x00填充补齐。

数据包字节序：服务器传来的字节序是小端的，解析时要小端解析，为统一字节序，这里构造包也使用小端构造，故wireshark
抓包中发现的banner 0x8223 在这里 为0x2382，小端构造数据包后仍以0x82 0x23 顺序发送数据包，resv也同理

工作流程：
（1）连接服务器
    I．UDP连接：
        每次运行IPClient时，客户端发送一个UDP连接请求包（server_port = 5301，num = 0x05），服务器收到后发送应答包
    （client_port = 5201，num = 0x06），若客户端收到服务器返回消息，则打印服务器返回的一句话（如：“尊师重道，敬业乐群。”），
    若在一定时间内没有收到服务器的应答包，则打印“服务器连接失败”。
    II．TCP连接：
        客户端主动connect服务器（server_port = 80），建立一个TCP连接。连接建立后，服务器向客户端发送版本信息，以此
    判断客户端是否需要更新版本。之后服务器主动请求断开连接。

（2）IP开放
        当用户输入用户名密码并单击IP外出放行按钮后执行。
        IP开放分两个来回，共计4个包。首先，客户端发送含用户名的开放请求包（server_port = 5300，num = 0x1f），服务器
    回复包含计算秘钥Key信息的包（client_port = 5200，num = 0x20），客户端根据Key，密码，用户名，利用MD5算法取得摘要
    密文，并将密文发给服务器（server_port = 5300，num = 0x21），服务器判断摘要密文是否正确，并回复告诉客户端是否开放
    成功（client_port = 5200，num = 0x22）。

（3）心跳包
        IP开放成功后，客户端每隔60秒和服务器交换一次信息，服务器以此确定此IP仍在开放状态，若一定时间服务器未收
    到客户端的继续开放的心跳包，则关闭此IP的开放状态。与此同时，服务器返回当前IP的使用流量与账号余额信息，IPClient
    打印到界面上。心跳包一个来回，共两个包，由客户端发起（server_port = 5301，num = 0x1E），服务器回复（client_port = 5201
    ，num = 0x1F）。

（4）IP关闭
        当用户单击IP外出截止按钮后执行。
        IP关闭分两个来回，共4个包。首先，客户端发送含用户名的关闭请求包（server_port = 5300，num = 0x14），服务器回复
    包含计算秘钥Key信息的包（client_port = 5200，num = 0x15），IPClient根据Key使用一定算法加密密码，发给服务器（port
    = 5300，num = 0x16），服务器判断密文是否正确，并回复告诉客户端是否关闭成功（port = 5200，num = 0x17）。
        注意：当前，已经不需要两个来回了，一个来回即可：客户端发送关闭请求包（server_port = 5300，num = 0x23）服务器直接回复
    客户端是否关闭成功（client_port = 5200，num = 0x24）

二、外网拨号
服务器IP：202.193.160.123
建立连接数：1
只需要本地的随机一个端口向服务器的20015端口发送60长度的udp数据包即可获得拨号权限，此后pppoe拨号即可上网，
而后每30s发一个心跳包给服务器20015端口，长度为56。
"""